NGINX Building
--------------
- 1. build SENG-SDK with external libs to have lwIP, SENG and co. libraries ready
- 2. run ../fetch_external_apps.bash to fetch and patch NGINX
- 3. build SENG-NGINX via `../build_seng_nginx.bash` from within nginx directory

Note: mini_nginx.conf will be auto-copied to build/config/

NGINX Benching
--------------
- build via 
- use 2 uworkers and separate workers SENG options if possible
- ./sbin/seng_nginx from build/

